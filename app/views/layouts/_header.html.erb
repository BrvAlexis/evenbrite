<nav class="navbar navbar-expand-lg bg-light" data-bs-theme="light">
  <div class="container-fluid">
    <%= link_to "Accueil", events_path, class: "navbar-brand" %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor03">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <%= link_to "Créer un événement", new_event_path, class: "nav-link" %>
        </li>

        <% if current_user && current_user.admin? %>
          <li class="nav-item">
            <%= link_to "Tableau de bord admin", admin_root_path, class: "nav-link" %>
          </li>
        <% end %>

        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Compte</a>
          <div class="dropdown-menu">
            <% if user_signed_in? %>
              <a class="dropdown-item" href="<%= edit_user_registration_path %>">Modifier Password</a>
              <a class="dropdown-item" href="<%= destroy_user_session_path %>" data-turbo-method="delete">Déconnecter</a>
              <a class="dropdown-item" href="<%= user_path(current_user) %>">Mon Profil</a>
            <% else %>
              <a class="dropdown-item" href="<%= new_user_registration_path %>">S'inscrire</a>
              <a class="dropdown-item" href="<%= new_user_session_path %>">Se connecter</a>
            <% end %>
          </div>
        </li>

        <!-- Intégration du menu Darktheme -->
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Catégories</a>
          <div class="dropdown-menu">
            <a class="dropdown-item d-flex gap-2 align-items-center" href="#">
              <i class="bi bi-music-note h1"></i>
              Musique
            </a>
            <a class="dropdown-item d-flex gap-2 align-items-center" href="#">
              <i class="bi bi-suit-heart h1"></i>
              Mariage
            </a>
            <a class="dropdown-item d-flex gap-2 align-items-center" href="#">
              <i class="bi bi-palette h1"></i>
              Art
            </a>
            <a class="dropdown-item d-flex gap-2 align-items-center" href="#">
              <i class="bi bi-activity h1"></i>
              Sport
            </a>
          </div>
        </li>
        <!-- Fin de l'intégration du menu Darktheme -->

      </ul>

      <form class="d-flex">
        <input class="form-control me-sm-2" type="search" placeholder="Cherche un évènement près de chez toi">
        <button class="btn btn-secondary my-2 my-sm-0" type="submit">Recherche</button>
      </form>
    </div>
  </div>
</nav>
